# Permission explanation

Active tab and scripting read only a page you invoke. Tabs builds the reviewed picker and invalidates navigated/closed context. Context menus provide selection actions. Storage keeps preferences and local conversation messages. Every website origin is optional and requested at use time. Optional clipboard, native messaging, and notifications activate only for their named user-selected features.
